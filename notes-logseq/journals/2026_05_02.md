- Trying to get shutdown working with rtc
	- I will first try the project with everything but removing rtc init
		- that fixed it. when it resets the time is midnight so the alarm goes off right away and provides and interrupt. I'm not sure why that matters though because we initialize it then wait 5 seconds. So after the initialization it interrupts then handles it then goes to shutdown so I feel it shouldnt matter.
		- Maybe I'm not resetting the interrupt properly in the RTC. Stepping with the debugger, it doesn't get to the interrupt callback. So the first time we setup the RTC it interrupts so pulls the pin low and when we reset we init again but we stay low as the interrupt was never handled
		- Working with exti documentation uses the standard HAL_GPIO_EXTI_Callback as I did in the past. The reference manual doesn't state any uses of HAL just mentions registers. The help manual that I had to dig throught he install files for doesn't state a callback function under exti. By going to the exti.c hal file and looking through the functions we now instead have two separate callback functions one for falling edge and one for rising edge. I used the falling edge one but it still didn't seem to work. The issue was when working with the debugger I can't do a POW so the RTC isn't reset and it might start with an interrupt already. By clearing the interrupt then init, it throws and interrupt and is properly handled. The power is now always super low and not 300uA. To note the noise provides 0.0-0.2 uA of power in the multimeter.
		- I think the issue before is that I had the old callback function which was never called. So an interrupt happened and a register recorded that, then we never handle it and we try to shutdown then it wakes up to standby or never gets to shutdown because there is already an interrupt.