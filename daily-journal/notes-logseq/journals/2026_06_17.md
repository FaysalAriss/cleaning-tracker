- Getting back into it. Read pervious day's notes to see why I'm messing with the eeprom and if it's an actual solution. Going to read the datasheet to know how to use the eeprom and take notes here for the future when implementing
	- Pages im going to look at
	- pg 11-14
	- pg 20-26
	- pg 31
	- pg 33
	- pg 35-41
	- pg 54-57
	- In the status and control registers theres an EEbusy bit and EERD bit to load config from eeprom to config registers daily
	- On power configuration registers refreshed from eeprom, but which ones are the configuration registers. I assume not the time as that would be stupid. But I hope the alarm registers as thats what's messing me up.
	- "The best practice method to use the Configuration EEPROM with RAM mirror registers at addresses 30h to 37h is to make all Configuration settings in the RAM first and then to update all Configuration EEPROMs by the Update EEPROM command." page 57
	- The configuration registers don't seem to include alarms. So if the alarm isn't overwritten why is there no interrupt and why does waiting a few seconds fix it. I will try the previous method with no wait at the start and then read the register for the alarm to see if it's been overwritten. Then i'll need a way to communicate that back to me as this only happens without the debugger.