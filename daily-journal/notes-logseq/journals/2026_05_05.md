- working on waking from shutdown when rtc interrupt
	- it doesn't seem like configuring the pins as a wakeup pin does anything which explains why I had to manually tell it to listen in blackpill-power. I changed a pin from a wakeup pin to default/reset and all it did was make it analog as it does for all the unused pins, it didn't remove any init for the pin as there was none
	- I wonder what happens when I enable wakeup on a pin used for something else
	- As usual there are no guides for HAL, looking at my previous work and looking at the pwr.c I found the function to enable the wakeup pin. However wakeup pin 7 wasn't in the comments as one I could select, looking through the .chm (help manual installed with cubemx or ide) I found a define for the pin. Compiles fine with interrupt still configured for that pin. I'll try it out.
	- Doesn't work, I will try with configuring as wakeup. Seems to work in the debugger, let me check with multimeter. Isn't working, somehow drawing higher current when trying to shutdown. I think the issue is that I removed the interrupt clearing at the start, I did that recognizing that since I fixed my interrupt function is there was an interrupt it would handle it. But now that it's no longer an interrupt pin it won't handle it. Also I'm curious if it would handle it even if it started as LOW as in no edge would be detected (I later realized: well if it wakes on an interrupt then yes it would wake and handle and if it was just set as a wakeup pin then no as its not an interrupt)
	- When we go to shutdown we call wake on interrupt, what if I just didn't bother with enabling the wake up pin. Let me try that. It doesn't fully shutdown probably because the RTC pin is normally high. I will go back to the previous approach
	- To enter shutdown mode one line in the reference manual says "WUFx bits are cleared in power status register 1 (PWR_SR1)" so I needed to clear the flag that says we were just in shutdown. That worked. But now it's on for 10s and shutdown for 5s. But the way I have it set up is that I set that I reset the interrupt in the RTC then set the time 10s from the alarm then wait 5s then shutdown. So it should be awake for 5s then shutdown then 5s from then it would've been 10s since the start of the timer and thus it would wake up.
	- Using the debugger I can see that the wakeup flag is going HIGH after I clear it. It seems it goes high when the appropriate signal is on the pin regardless if it's awake or not. Currently the RTC interrupt seems to be low at the start so I reset the flag then after handle_interrupt it still low for some reason and inside of init I enable the wake up pin while the pin is still low so it flips on the flag. So then when it goes to shutdown it restarts and now the interrupt pin is high so it goes through properly and sleeps so it sleeps double the delay time. I noticed this also when I tried a delay of 8s and it waited 16s. It seems the first time it does this though the current actually increases from 1.2mA to 1.5mA during the second phase (second delay) but in subsequent times it stays at 1.2mA. Currently now sure why but maybe I can find out when I know more
	- There are still a few problems. Why does the RTC start with an interrupt (low), why does enabling the wake up pin put it back to high and not the resetting of the interrupt. Why does it loop just twice as when I looked more carefully at the second time the interrupt pin is still low when enablewakeuppin happens so it's not what I explained. But the second time around SBF (which is set when going to standby or lower power modes) is high. I doubt the RTC actually does start with an interrupt since handling the interrupt doesn't change it. It seems it is pulled by the MCU and then pulled high when it realizes we want to check for a low signal?
	- I will first try also clearing SBF and see if it always exists shutdown instantly. Yes but because it creates more weirdness.
	- I moved clearing the powerflag right before shutdown and now it seems to work except on POR. It works on manual reset. I also still don't understand why the RTC interrupt is low at the start until the enablewakeuppin line, specifically when I enable the wakeup.
	- I have checked the pull up and down resistors for wake and none are active.
	- Measuring the pin when it says 0 on the debugger it's actually high, it doesn't seem to pull the signal just ignore it during setup. Which is not mentioned anywhere or I have yet to find it. And for some reason on POR it doesn't wake up after the first time and I can't seem to debug that since the debugger doesn't do a POR it does a NRST. And I can't measure to figure it out because when measuring the leads of the multimeter affect the pin enough to cause it to wake up and run properly.
	- For next time: what if i trigger an interrupt, don't clear it then call enablewakeuppin will it still go high?
	-
	-