already used both gnd pins on the board. moved to a breadboard to share gnd and for the buttons. checked how the buttons are connected using a multimeter. to check if button works i added a led with it. measure forward voltage using multimeter in diode mode and calculator for resistance. tested with vcc to make sure it works before using gpio.

I configured the pins as input, with pull up resistors as when it gets pressed they go to gnd. The leds don't light up, we get 3.3V and the pull resistor is typically 40k ohm according to the datasheet so max of 0.0825 mA, not enough to light it up. I'll just omit it. Looking at how they used the blue push button on the nucleo they used an interrupt which does make more sense to use, i will do the same. Had it connected wrong it was always "pushed". Connected properly only one of them worked, one was pulled up just to 0.65V. Or maybe I was shorting them again, probably. It works with a different pin or because i fixed it.

Now a new problem, debouncing. One push of a buttons can be sensed as multiple as the internals of the button bounce. This is a very common problem so I will search for solutions online, preferably using hardware as it's more fun, I think a cap will do. 


shot ideas: bending pins, cutting pins, inserting into breadboard, taking wires out of drawer/3d printed holder, oscilloscope button bounce, 