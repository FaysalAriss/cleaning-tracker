Using boot0 and nsp seemed to work reliably. And I can't let myself use something I dont understand so time to figure them out as well as connection under reset.

When the mcu starts it picks either between running the program or a bootloader. But if we tie boot0 high then it runs the bootloader which will be ready to take instructiosn such as flashing the code. If the mcu is awake the programmer can take control through the configured debug pins and program it. But due to it being in standby its basically dead and the debug pins aren't being monitored.

NRST is simple enough its a reset pin with an embedded pull up resistor. Its an active low pin so thats why its pulled high. So I just need to press it so the mcu restarts and the bootloader runs.

The connection modes are a little scattered in the documentation but what I could get is:
- Connect under reset: uses a reset vector (pointer to reset handler) to connect before any code has run
- Software reset: reset the mcu using software interrupts
- Hardware reset: reset the mcu using the nRST pin, but we need to connect that pin to the debugger/programmer
- Core reset: resets just part of the mcu

So if I use hardware reset and connect the reset pin to the programmer it seems that i dont need to touch anything during the programming. That doesn't seem to work. The docs specified using jtag and pin 15, but i have reset pin on the stlink ill look at the signal using the oscilloscope. Researching further all the usb stlinks are clones so the reset pin doesn't work.

I am curious why i can keep boot0 high and the program still runs after programming though. I wasn't connecting boot0 to 3.3v i though it pin a4 because the datasheet puts the pin names for many packages. Even when pulling the actual boot0 high it doesnt seem to change anything. It seems the programming was working because I was using connection under reset and when i reset the board it programmed it before any code ran. Maybe it realises there is code to run and does so but the documentation shows it goes in a loop until it receives a signal, maybe timeout, I have no idea... I'm just gonna move on.


Im checking out the power flags to make sure I know what I need for the final thing. Im concerned about being able to figure out which wakeup pin woke up the mcu. Im going to make a new project using an mcu with multiple wakeup pins to see if the zdocumentation has anything. Beautiful :D :
```
/** @brief  Clear a specific PWR flag.
  * @param  __FLAG__ specifies the flag to clear.
  *          This parameter can be one of the following values:
  *            @arg @ref PWR_FLAG_WUF1 Wake Up Flag 1. Indicates that a wakeup event
  *                  was received from the WKUP pin 1.
  *            @arg @ref PWR_FLAG_WUF2 Wake Up Flag 2. Indicates that a wakeup event
  *                  was received from the WKUP pin 2.
  *            @arg @ref PWR_FLAG_WUF3 Wake Up Flag 3. Indicates that a wakeup event
  *                  was received from the WKUP pin 3.
  *            @arg @ref PWR_FLAG_WUF4 Wake Up Flag 4. Indicates that a wakeup event
  *                  was received from the WKUP pin 4.
  *            @arg @ref PWR_FLAG_WUF5 Wake Up Flag 5. Indicates that a wakeup event
  *                  was received from the WKUP pin 5.
  *            @arg @ref PWR_FLAG_WUF7 Wake Up Flag 7. Indicates that a wakeup event
  *                  was received from the WKUP pin 7.
  *            @arg @ref PWR_FLAG_WU Encompasses all five Wake Up Flags.
  *            @arg @ref PWR_FLAG_SB Standby Flag. Indicates that the system
  *                  entered Standby mode.
  * @retval None
  */
#define __HAL_PWR_CLEAR_FLAG(__FLAG__)   ( (((uint8_t)(__FLAG__)) == PWR_FLAG_WU)  ?\
                                           (PWR->SCR = (__FLAG__))                  :\
                                           (PWR->SCR = (1U << ((__FLAG__) & 0xFFU))) )
```
There are flags for each, I am golden.

At this point I think I know everything I need to know to choose the final components. I know how to calculate how much RAM i need for the mcu, I need enough space for the fonts and images if I add any in the ROM, I know the communication protocols I need, I need backup registers, every other feature I don't need so I will prioritize minimum power then cost. I need a I2C low power RTC with an alarm. I need a SPI eink screen of the size and color I want. I need push buttons and schmitt triggers for debouncing them. I need a battery holder. I also want to make sure I have a way to measure power and cut things off so I will use jumper pins? the piece of plastic with metal inside to short two pins.

I will begin with the screen as it will decide the MCU to pick, then the RTC, buttons, schmitt trigger, battery holder and misc specialties such as the jumper pins.

The screen i chose is:
https://buy-lcd.com/products/37-inch-416x240-e-paper-black-and-white-spi-fast-refresh-electronic-eink-display-screen-esl-gdey037t03?VariantsId=10346
https://www.good-display.com/product/437.html
3.7 inches is a good size. I like the dimensions which are more rectangular. I also like that the connector goes from the bottom as I will be using it vertically. The partial refresh will be nice for when a user clicks a button so they can quickly see the screen change. Others say "low power" and have the same standby power. Goodisplay has good documentation and all the epapers seem similar price so why not choose the easiest to use. The resolution is 416x240 and has 4 grayscales (just BW) so 2 bits per pixel so 25kB ram needed which easily fits in a stmU0.\
Along with it I'm getting https://buy-lcd.com/products/development-kit-connection-adapter-board-for-eaper-display-demo-kit to test the screen on its own if need be. Its inexpensive and they've provided the schematics which I will copy for my board. I will need to get a general idea of what the components do so I can design/layout everything optimally. I know einks need higher voltages than 3.3 so they use some kind of boosting which might need to be considered. Although at these low currents it shouldn't matter much. The speciality components from this board as in aren't so common I have them on hand are the MBR0530 and SI1308EDL which I will need to order. I will also get the 10uH inductor.

With this I look through the U0 line: https://www.st.com/en/microcontrollers-microprocessors/stm32u0-series/products.html. The U03 lines doesn't have enough ram but everything else does. Otherwise all of them have what I need, just for peace of mind ill choose one with the most flash (for a whopping $0.82 higher than the cheapest, i will go broke) https://www.lcsc.com/product-detail/C22450589.html?s_z=n_stm32u0

I was initially looking at the most precise clocks. But then I took a step back, especially when the one I found was going to be almost the same price as the mcu, and looked at my requirements. If the screen resets at 12:15 instead of 12:00 it will not matter in the slightest. So instead the cheapest which were in chinese, didnt have an alarm or internal oscillator, from one extreme to another. I really couldn't find an inbetween. I was able to find lots which needed an external clock but I didn't want to deal with that for the first version. The ds3231 that im currently using draws like 100uA which is way too much for me and is still a few dollars. I'm going to back to my original pick which is cheaper, more accurate and uses way less power. It's the RV-3028-C7: https://www.lcsc.com/product-detail/C3019759.html. There is a QA version = automotive. This is QC = commercial.

NOTE: make sure to look at what components each sensor needs, especially the mcu, walk through the steps im gonna take when connecting and testing everything

I will also get a real stlink for the future https://www.lcsc.com/product-detail/C85147.html