With the connector done now I need to wire it up. But that means I need to configure the project, the given example uses a different chip and mine isn't compatible for a conversion so I will start a new project copying the same config and copying the files. They use the MCU as transmit only which makes sense as we only write to the screen, there is one input but its not part of the spi lines. The busy line. We also manually turn on CS using their config as we dedicate a gpio to it.

I copied over the files under the "User" folder since they seem to be the right ones but looking around there are some RTOS files and there is a drivers folder so I don't know yet. Then using https://wiki.st.com/stm32mcu/wiki/STM32StepByStep:Step4_Sensors_usage as a reference I imported the files/updated paths. After including each path individually I stopped getting not included errors. Then I had to change the pin numbers in a .h file. Then I get that the .rodata doesn't fit in the flash. That section stores constants such as fonts and images. I simply commented out a line which displayed an image so the compiler optimized it away.

After wiring I booted it up and it flashed a bunch of colors to clear then did nothing. In the debugger I realized we don't have enough RAM as malloc is returning null. Messing around with the size didnt yield expected results, as in changing height and/or width only changed the height. I was still able to see some cut off text so I know the library and screen is good. My selection of MCU will have enough ram for the eink I select so although I could try and hack this together ill simply use another board for testing. I will use the stm32f401rc which has 64kB of ram. We need 120kB to drive all the colors but for one of four we only need 30kB so this is fine. 600*400 pixels, 4 bits per pixel, 8 bits per byte, 4 colors = 30kB. They say the screen should be refreshed at least once a day and that takes a few seconds of it flashing. To make this non disruptive it would make sense to do it in the middle of the night so I will need a way to set the hour on the rtc through buttons.

It seems this board is called the "black pill". This resource was helpful: https://stm32world.com/wiki/Black_Pill. It let me know the location of LED and I was confused about selecting a clock signal but the video showed how to. Blink works. Now to redo the config for the eink. It works :D