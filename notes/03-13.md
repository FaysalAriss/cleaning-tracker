Continuing tutorial: https://www.youtube.com/watch?v=aVUqaB0IMh4

DRC check and fixed the small issues. No need to setup rules check, easyeda uses jlc's automatically. Can't set bottom layer to pwr plane. Using two tabs to reference schematic while routing. Set track widths: design>design rules> make new category under physics>track then i can define width and set the appropriate nets to use that one. Started the layout, 2 rtc lines on the right but 1 on the left. And all the epd stuff at the bottom so they need to run through each other. And lots of dense lines at the fpc connector.