As ordering the components and pcb + shipping takes the longest I want to make the simplest prototype with all the functionality. That way I know how everything will mesh together and what I need to buy. So I will continue using the given library and fonts. That way while waiting for parts to come I can work on the software. The things left to do is try out sleep mode + wakeup using interrupt, running on a battery, add two buttons and make them update the screen appropriately, and possibly add a hour led. I'm thinking if I do want the screen to refresh at midnight I would need to be able to set the hour, so if i press and hold the two buttons it can display the current hour. But I don't think it can refresh quickly enough so that it displays the new hour with each button press. So the user would see the time and know how many times to press but without feedback you don't know if anything is happening so each time the hour increments the led could flash or maybe they could just check the time again at the end. I will implement it anyway and I could always just not solder it. Currently when refreshing the screen after a button press I will use a placeholder "days gone by" number as I can fetch the days from the rtc at a later date.

I will begin by making the main screen UI. To get a better understanding of what I could do I tried to go through the code but on init it sends a bunch of magic numbers which are not documented in the manual. Asking AI it seems there a driver built into the glass and they never mention the name of it anywhere. The most common chip doesn't seem to have some of the commands it sends so I'm just gonna leave it be. They made the screen, they know how to work with it. Actually I think im just reading it wrong, with more time I could figure it out, but doesn't matter much right now. Heres what I could extract from reading the code (for future reference):command:

- 0x10 = send pixel data (starting (0,0))
- 0x04 = power on
- 0x12 = refresh display
- 0x02 = power off
- 0x07 = deep sleep, need to send data 0xA5 after

After sending the pixel data they call turnOnDisplay. This turns on the display, sets some settings and then when they send the refresh display command to the driver it displays the image. Then it turns off the screen. So we first write to the display buffer then display it all at once.

They divide width and height by 2 when making a new image. my best understanding of this is that the library was made for black white pixels where there are 8 pixels per byte but ours is 2 pixels per byte so we essentially divide by 4. This is in the generic GUI paint library so it makes sense. Looking more its because they take in a UWORD which is 2x the size of the UBYTE our width/height are in. This also causes us to have half the resolution so when putting text at x = 150 we get it in half the screen even though we have 600 pixels. For the files specific to this eink the math is more clear.

Ok whatever, I'm just gonna make some garbage temporary UI and move on. I'm gonna keep a nice file structure for the future though. I made a header file which spawns in with:

```
#ifndef UI_H_
#define UI_H_

#endif /* UI_H_ */
```

It makes it only define UI_H_ if it hasn't yet been defined. Avoids redefining when multiple files include this header. So when we enter the first we need to define, even if it has no value so then we skip it in the future.
Going through this I also realized that I don't need large ram to do this. The way they do it is make the image in ram then send it to the screen's buffer then refresh the image. But instead I can directly send it to the buffer. This will take some logic as the buffer takes in a pixels at once so if im adding a pixel i cant just go to that row and column and set it i just need to be there.
