- Got a thorough understanding of how to work with the EEPROM and realized it's only a few settings that get stored there, not including the alarm so that shouldn't be causing an issue
	- Still useful to set EEPROM so the only setting I do set in there (fd=111) isn't reset at midnight
- I wanted to confirm the behaviour that I thought was happening from what I read from the previous journals which I thought was it doesn't work on first boot.
	- Without the delay at the start it's awake for 5s then goes to sleep indefinitely since the RTC doesn't throw an interrupt.
	- With the delay it's awake, 8s awake (+3 seconds because of the newly added delay), 15 seconds asleep.
- Ok so the issue is with the RTC not throwing an interrupt (i believe). Let me comment out any power related code and confirm that this is the case.
	- I wish to check if there's an interrupt after 20 seconds and maybe more but I can't do that with SWD as it will flash and reset the board before beginning removing the fault. I will just measure by hand and the interrupt should stay as nothing should be handling it. I've removed handling the interrupt from the callback and in the while loop
		- It actually is throwing an interrupt, so the problem is with the MCU not waking for it
-
- My current thoughts:
	- How to test without ammeter as it could be causing issues
	- Where is this 290uA coming from, possibly from RTC interrupt 10K pullup being pulled low