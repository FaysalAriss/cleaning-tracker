- Going to init only on POR
	- Followed my notes from backlog.md
	- ```
	    if(__HAL_RCC_GET_FLAG(RCC_FLAG_PWRRST) == SET){
	  	  do{ status = RTC_init(&hi2c1); HAL_Delay(10); } while(status != HAL_OK);
	  	  __HAL_RCC_CLEAR_RESET_FLAGS(); //clear so it doesn't trigger on next cycle
	    }
	  ```
	- Works great, checked using SWD. First time PWRRSTF is high, then clears it and in subsequent times its low and this part gets skipped
	  id:: 6a7be8c0-54bb-4b49-acaf-b8196f859a71
		- As SWD doesn't trigger a POR at least under "Connection under reset" I added a delay before processing the flag so by the time I clicked debug and it flashed it still wouldn't have finished.
		- Now I can always clear WUF7 in RTC init
		- With all of that it all works great, on POR it initializes and never again. dayCount is only incremented when the RTC wakes the MCU, so should truly line up with the days passed.