- still working on making rtc wakeup mcu from shutdown
	- the reason the pin goes high only when I call enablewakeuppin is probably because the pin just isn't configured before then. as I said in the previous entry when I mark the pin as wakeup in cubemx it doesn't initialize or do anything to it. So the mcu isn't going to bother reading that pin. But when I tell yo im going to use this pin for wakeup it'll start monitoring it
	- Im looking at the reset values of the RTC to see if maybe that's causing anything since I was having problems with a POR. I noticed the month and date start 1 and not 0 as I had programmed them so I changed that to not produce any undefined behaviour.
	- I cleaned up the code a little and it still doesn't exit shutdown mode on a POR. I disconnected the cable and plugged it back in expecting 1.2mA but it was still in shutdown mode and I realized it was still running, I measured the voltage and it was at 2.2V and slowing going down. It uses such little power in that state that it has voltage for a minute or more.
	- It seems no interrupt is getting sent by the RTC since we go into shutdown but never exit. As measuring during shutdown causes it to get out of it. I will measure beforehand and see if it sent an interrupt before going to sleep that never gets handled. By putting the probe on before it shutdown I could keep it there without it waking back up although it did add like 300uA. It simply stayed at 3V so indeed the RTC was not putting out an interrupt.
	- I'm thinking maybe the initial communication to set the time/setup the alarm went wrong. I never capture the return value, I will do so and not shutdown if that happens as a signal as I don't really have a way to communicate externally. Still the same.
	- It seems the initialization gets overwritten by the eeprom mirror, adding a 3 second delay to the very start confirms this as it now works. It now uses around 4.2uA during shutdown though which is weird. I will fix the eeprom issue first through by writing to the eeprom so I don't have the reinitialize the settings every time.
		- It takes around 66ms to do this mirroring and in that time the EEbusy bit is set. So I could also just wait for that to be low, but writing to it seems like the better option.
		- 4.6.3 and 4.6.9 states how to set the eeprom values. First I need to disable the auto eeprom refresh which happens once a day to keep the integrity of the data. Then I set the settings in RAM and give two commands to store to the eeprom.